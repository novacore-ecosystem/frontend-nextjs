/** Moved to `@novacore/frontend-foundation` (framework-agnostic, shared with non-shadcn apps) — re-exported here so existing imports keep working. */
export {
  AUTH_COOKIE_NAMES,
  buildInitialAuthState,
  type InitialAuthState,
  type InitialAuthStateCookies,
} from "@novacore/frontend-foundation";
